table:
  name: view_event_attribute_value_index
  schema: public
select_permissions:
  - role: internal_service
    permission:
      columns:
        - composite_key
        - key
        - block_height
        - event_id
        - index
        - tx_id
        - value
      filter: {}
      limit: 100
