table:
  name: coin_transfer
  schema: public
select_permissions:
  - role: internal_service
    permission:
      columns:
        - id
        - block_height
        - tx_id
        - tx_msg_id
        - from
        - to
        - amount
        - denom
        - timestamp
        - created_at
      filter: {}
      limit: 100
