table:
  name: account
  schema: public
object_relationships:
  - name: account_vesting
    using:
      foreign_key_constraint_on:
        column: account_id
        table:
          name: account_vesting
          schema: public
