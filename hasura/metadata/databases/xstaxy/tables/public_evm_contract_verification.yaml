table:
  name: evm_contract_verification
  schema: public
select_permissions:
  - role: internal_service
    permission:
      columns:
        - files
        - abi
        - code_hash
        - compile_detail
        - compiler_setting
        - compiler_version
        - contract_address
        - contract_name
        - created_at
        - creator_tx_hash
        - status
        - files
        - id
        - abi
        - compile_detail
        - created_at
        - status
        - updated_at
      filter: {}
      limit: 100
