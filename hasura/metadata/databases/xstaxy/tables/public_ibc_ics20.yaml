table:
  name: ibc_ics20
  schema: public
object_relationships:
  - name: ibc_message
    using:
      foreign_key_constraint_on: ibc_message_id
select_permissions:
  - role: internal_service
    permission:
      columns:
        - id
        - ibc_message_id
        - sender
        - receiver
        - amount
        - denom
        - status
        - channel_id
        - sequence_key
        - type
      filter: {}
      limit: 100
